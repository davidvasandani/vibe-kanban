# Technical Spec: Recover Missing Codex Conversations

The authoritative stage-1 technical specification is maintained in the
repository-root `SPEC.md` for this pipeline run. It specifies a narrowly
classified `thread/fork` not-found fallback to `thread/start` in the same Vibe
Kanban workspace, normal replacement-session persistence, and fail-loud
behavior for every unrelated error.
