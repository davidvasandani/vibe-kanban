# Prior Knowledge: Missing Codex Conversation Recovery

The required workspace-root distillation is `../../../PRIOR_KNOWLEDGE.md` at
the shared task workspace. The relevant project rules are: Codex continuation
depends on resolvable rollout files; missing private rollout state cannot be
reconstructed; only the exact missing-conversation error may recover; and a
replacement thread must use the ordinary registration and turn-start path.
