# Knowledge Capture: Desktop Deploy Status

Reusable knowledge was added to
`wiki/flexible-collapsible-panel-stacks.md` and its `wiki/INDEX.md` description
was refreshed. The page now records how to place intrinsic, non-collapsible
desktop chrome in `RightSidebar` without leaking it into the mobile Git-tab reuse
or stealing flex height from expanded drawer sections. It is tagged `VAS-377`.
