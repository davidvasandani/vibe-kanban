# Data Model: Expand Mobile Workspace Toolbar

No persisted or domain data is introduced or changed. The existing
`MobileTabDefinition[]`, active tab identifier, callbacks, and navbar props are
unchanged.
