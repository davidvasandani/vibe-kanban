# Clarifications

- A running turn finishes normally; confirmation queues rather than interrupts.
- Existing queued user input is never replaced by the synthetic restart prompt.
- The synthetic prompt is visible as an ordinary continuation for auditability.
- No open questions remain.
