# Data Model

`LegacyIdentifierMigration`

- `legacy_name`: persisted native key
- `identifier`: canonical protocol key
- `display_name`: preserved human label
- `profiles`: affected base executors

The migration is derived state and is never a second MCP registry.
