# Contracts

- GET remains read-only and returns an actionable safe representation or an
  unresolved collision.
- POST preflights the complete migration set before native writes.
- Operational responses use the safe identifier; display surfaces use the
  preserved label.
- Errors never include headers, environment values, or definitions.
