# /speckit.clarify

Review `specs/vk/fe2d-warn-when-starti/spec.md` and resolve underspecified areas. If answers are provided in $ARGUMENTS, fold them in and remove the matching `[NEEDS CLARIFICATION]` markers. List any questions that remain open.
