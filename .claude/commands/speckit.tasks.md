# /speckit.tasks

Read `specs/vk/fe2d-warn-when-starti/plan.md` and `.specify/templates/tasks-template.md`. Write `specs/vk/fe2d-warn-when-starti/tasks.md`: dependency-ordered tasks with stable `T###` ids, `[P]` on tasks that touch independent files (parallel-safe), and the exact file path(s) each task changes.
