# /speckit.constitution

Create or update the project constitution at `.specify/memory/constitution.md`. Use the templates under `.specify/templates/` for structure if helpful. Capture the principles in $ARGUMENTS (or refine the existing ones). Keep it concise and enforceable.
